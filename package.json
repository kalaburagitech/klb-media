{
  "name": "klb-media-services",
  "version": "2.0.0",
  "description": "Production media platform: Cloudflare R2 + Convex + FFmpeg transcoding.",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix frontend",
    "dev:frontend": "npm run dev --prefix frontend",
    "dev:worker": "npm run dev --prefix workers/media-api",
    "dev:transcoder": "npm run dev --prefix services/transcoder",
    "install:all": "npm install && npm install --prefix frontend && npm install --prefix workers/media-api && npm install --prefix services/transcoder",
    "build:transcoder": "npm run build --prefix services/transcoder",
    "deploy:worker": "npm run deploy --prefix workers/media-api"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
